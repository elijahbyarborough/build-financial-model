---
name: build-model-phase-6
description: |
  Phase 6 -- Returns. Builds the Returns tab with entry/exit pricing, XIRR, and multiple-based returns.
  Load after `build-model` and `firm-formatting` are already in context.
---

# Phase 6 -- Returns

## Before Starting This Phase
1. Ensure `build-model` and `firm-formatting` skills are loaded
2. Read the Task Tracker tab to restore model state (current phase, flags, Lease BS Map, etc.)

## After Completing This Phase
1. Update Task Tracker: mark Phase 6 complete, record all outputs and state
2. Clear context (start new conversation)
3. Next phase: load `build-model` + `firm-formatting` + `build-model-phase-7`

## Phase Instructions
See `phase-6-returns.md` for the complete phase reference.

## Methodology Excerpts
- `meth-projection-length.md` -- Projection length calculation and historical period guidance
