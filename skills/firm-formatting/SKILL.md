---
name: firm-formatting
description: |
  FC firm-wide formatting and visual standards for Excel models. Single source of truth for visual hierarchy, number formatting, color coding, and all formatting conventions. Load at the start of every session alongside build-model.

  Triggers on: any Excel model, spreadsheet formatting, financial model reformat, equity research output, valuation output, or any task where visual consistency of financial outputs matters. Also triggers when the user says "reformat", "clean up this model", "firm format", or "standardize this".
---

# Firm Formatting Standards

This skill contains the complete FC firm formatting specification. See `firm-formatting.md` for the full reference.

**Load this skill at the start of every modeling session alongside `build-model`.** These formatting rules apply to every cell operation across all phases.
