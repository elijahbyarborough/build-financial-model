---
name: build-model-phase-5
description: |
  Phase 5 -- Model Tab. Consolidates key outputs onto the Model tab including valuation multiples, ROIC/ROTIC, and summary metrics.
  Load after `build-model` and `firm-formatting` are already in context.
---

# Phase 5 -- Model Tab

## Before Starting This Phase
1. Ensure `build-model` and `firm-formatting` skills are loaded
2. Read the Task Tracker tab to restore model state (current phase, flags, Lease BS Map, etc.)

## After Completing This Phase
1. Update Task Tracker: mark Phase 5 complete, record all outputs and state
2. Clear context (start new conversation)
3. Next phase: load `build-model` + `firm-formatting` + `build-model-phase-6`

## Phase Instructions
See `phase-5-model-tab.md` for the complete phase reference.

## Methodology Excerpts
- `meth-roic.md` -- ROIC & ROTIC analysis formulas and presentation
